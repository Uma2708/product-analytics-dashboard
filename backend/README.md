# Backend

Express + TypeScript backend starter with Prisma for Product Analytics.

Quick start:

```bash
cd backend
npm install
cp .env.example .env
# set DATABASE_URL and JWT_SECRET
npx prisma generate
npm run dev
```
